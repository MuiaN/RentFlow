## Packages
recharts | Data visualization for the dashboard revenue chart
date-fns | Date formatting and manipulation
lucide-react | Icons for the UI

## Notes
Using frontend-only React Context for state management as requested (no @tanstack/react-query or real API endpoints).
Mock data is placed in `client/src/lib/mockData.ts` to strictly follow the "ONLY write files in client/ directory" constraint, instead of `@shared/mockData`.
